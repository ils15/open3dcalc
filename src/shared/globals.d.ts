/**
 * Ambient globals injected at build time by Vite's `define` block
 * (see vite.base.config.ts).
 *
 * Never hardcode a version literal in the UI — read APP_VERSION from
 * src/shared/version.ts instead.
 */
declare const __APP_VERSION__: string;
